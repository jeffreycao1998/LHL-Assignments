// const io = require('socket.io')(8000);

const p1 = {
  name: 'Jim John',
  ready: false,
  
}

const p2 = {
  name: 'Pit Pat'
}

// io.on('connect', socket => {
//   console.log(`${socket.id} has connected`);

//   socket.on('name', data => {
//     console.log(`hi ${data}`);
//   })
// })