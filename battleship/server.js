// const io = require('socket.io')(8000);

// io.on('connect', socket => {
//   console.log(`${socket.id} has connected`);

//   socket.on('name', data => {
//     console.log(`hi ${data}`);
//   })
// })